name: Publish from Editor
on: 
  workflow_dispatch: 
  
# permissions: 
#   content: write
#   pages: write
#   id-token: write
# concurrency: 
#   group: "pages"
#   cancel-in-progress: false

jobs: 
  publish: 
    runs-on: ubuntu-latest
    steps: 
      # - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v3
        with:
          version: 8
      - name: Install Dependecies for PNPM
        run: | 
          pnpm install @actions/core
          pnpm install @actions/github
      - name: Generate Files from Editor
        uses: ./.github/workflows/actions/generate
        with: 
          example-param: Hello World
      # - name: Upload Generated Files
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: generated-output
      #     path: output-folder/
